//DERS 7..

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // int myNumber , myNumber2, myNumber3;
    // myNumber = 7 + 12;
    // myNumber2 = myNumber + 1;
    // myNumber3 = myNumber + myNumber2;       // bir variableyle bir değeri birleştirebilirsin...
    // printf("My number is : %d\n", myNumber);
    // printf("My number2 is : %d\n", myNumber2);
    // printf("My number3 is : %d\n ", myNumber3);

    // return 0 ;


    // ARTİMETİK OPERATÖRLER:

    // int x , y ,sum, sub , multi , div, mod;
    // x = 5;
    // y = 2;
    // sum = x + y;
    // sub = x - y;
    // multi = x * y;
    // div = x / y;
    // mod = x % y;
    // printf("sum : %d\n", sum);
    // printf("sub : %d\n", sub);
    // printf("multi : %d\n", multi);
    // printf("div : %d\n", div);
    // printf("mod : %d\n", mod);


    int myNumber;
        myNumber = 7;
        printf("myNumber : %d\n", myNumber);

    myNumber++;     // bunun mantığı bir arttır demek
        printf("myNumber++ : %d\n",myNumber);

    ++myNumber;
        printf("++myNumber : %d\n", myNumber);

        return 0 ; /* ++myNumber = ilk başta bir arttır sonra karşı tarafa eşitle*/
                    /* myNumber++ = lik başta karşı tarafa eşitle sonra bir arttır*/
                    



}